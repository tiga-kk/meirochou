# ComiPath Public Webapp Development Guidelines

このファイルには、`comipath-web` リポジトリにおける開発・運用方針を記述します（グローバルルールと重複する内容は除外しています）。

## 1. 開発ガイドライン

- **設計と計画の集約**:
  設計文書や実装計画は `docs/plans/` 配下に作成・配置し、これらを指針として段階的に実装を進めます。
- **モジュール・ファイルの分割**:
  ソースコードは関心や機能ごとにこまめにファイルを分割し、単一ファイルが肥大化したり役割が重複したりすることを避けます。
- **細粒度コミット**:
  コミットは機能ごとなどの小さな単位でこまめに行い、常に動作検証やテストがパスする状態を記録します。
- **Biomeによる品質管理**:
  大きな変更のコミット前には、必ず `biome check` を実行してエラーや警告が完全になくなるまでコードをリファクタリングします。
- **エージェントの役割分担**:
  実装作業は原則としてサブエージェント（`self` など）を起動して並行あるいは逐次的に行わせ、親エージェント（メイン）は設計・指示・コードレビューおよび検証結果の確認に注力します。

## 2. ビルド・検証コマンド

通常開発では、以下の検証プロセスをコミット前に実行します。

* 静的解析・型チェック: `npm run check:webapp`
* 本番ビルド: `npm run build:webapp`
* アセット契約検証: `npm run verify:webapp:build`
* Biomeチェック: `npx biome check`
